{
	"modListVersion": 2,
	"modList": [{
		"modid": "${modId}",
		"name": "${modName}",
		"description": "Makes GT: New Horizons world generation a pure function of the world seed. Fixes 28 sources of per-launch and per-route variance across 11 mods, so the same seed produces the same world on every launch and by every approach route. Adopting the jar re-rolls seeds once per jar version; existing saves are safe, because only newly generated chunks are affected.",
		"version": "${modVersion}",
		"mcversion": "${minecraftVersion}",
		"url": "https://github.com/OrderedSet86/gtnh-determinism",
		"updateUrl": "",
		"authorList": ["OrderedSet86"],
		"credits": "",
		"logoFile": "",
		"screenshots": [],
		"parent": "",
		"requiredMods": [],
		"dependencies": [],
		"dependants": [],
		"useDependencyInformation": false
	}]
}
